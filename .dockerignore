# Keep the Docker build context small. Only the backend module and the
# migrations directory are needed to build the Orbit backend image.
frontend/
scripts/
.git/
.github/
**/.DS_Store
**/node_modules/
backend/data/
*.log
README.md
